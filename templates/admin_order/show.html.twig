{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ order.createdAt ? order.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ order.total }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ order.Status }}</td>
            </tr>
            <tr>
                <th>OrderCode</th>
                <td>{{ order.OrderCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_order_index') }}">retour à la liste</a>

    <a href="{{ path('app_admin_order_edit', {'id': order.id}) }}">Changer Statut</a>

    {{ include('admin_order/_delete_form.html.twig') }}
{% endblock %}
